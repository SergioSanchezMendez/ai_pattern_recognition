% Calcula la distancia interset de cada 
% clase de X
function s = intersetDist(X, C)    



if (nargin ~= 2)
   error('Numero de argumentos incorrecto');
end

if (size(a,1) ~= size(b,1))
   error('A y B con dimensionalidad diferente');
end

%Algebra lineal
%d = norm(A - B)

d = sqrt(sum( (a - b) .^ 2 ))

end
